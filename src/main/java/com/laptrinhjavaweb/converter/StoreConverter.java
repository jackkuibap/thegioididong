package com.laptrinhjavaweb.converter;

import com.laptrinhjavaweb.dto.StoreDTO;
import com.laptrinhjavaweb.entity.StoreEntity;

public class StoreConverter extends AbstractConverter<StoreDTO, StoreEntity>{

/*	private ModelMapper modelMapper = new ModelMapper(); 
	
	public StoreDTO convertEntityToDto(StoreEntity entity) {
		StoreDTO storeDTO = modelMapper.map(entity, StoreDTO.class);
		return storeDTO;
	}
	
	public StoreEntity convertDtoToEntity(StoreDTO dto) {
		StoreEntity storeEntity = modelMapper.map(dto, StoreEntity.class);
		return storeEntity;
	}
*/
}
